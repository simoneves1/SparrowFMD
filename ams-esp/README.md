# ams-esp

Deferred — not required for v1. Placeholder so the repo structure
doesn't need to change shape later.

## Contents (planned, once picked up)
- UART client to main-esp, same pattern as touch-ui.
- Spool selector control, filament runout sensing, swap-at-pause
  handling.
- Specific ESP chip not yet chosen — see the v1 scope doc.
